# High-level dependency constraints (HIGH-9)
#
# This file contains version constraints (~=) for direct dependencies.
# For deployments, use requirements-lock.txt which has exact pinned versions.
#
# To update dependencies:
# 1. Edit this file with desired version constraints
# 2. Install in clean venv: pip install -r requirements.txt
# 3. Regenerate lock file: pip freeze > requirements-lock.txt
# 4. Update Dockerfile if needed
#
# See: TECHNICAL_DEBT.md HIGH-9 for rationale

# Core Django
asgiref~=3.8.1
Django~=5.2.2
django-browser-reload~=1.13.0
python-decouple~=3.8
sqlparse~=0.5.1

# Security & PHI Compliance
django-encrypted-model-fields~=0.6.5
django-auditlog~=3.0.0
cryptography~=42.0.0

# API Layer (Django REST Framework)
djangorestframework~=3.15.0
djangorestframework-simplejwt~=5.3.0
django-cors-headers~=4.3.0
drf-spectacular~=0.27.0

# Background Tasks & Async Processing
celery~=5.3.4
redis~=5.0.1

# Database (production)
psycopg2-binary~=2.9.9

# Production Server
gunicorn~=23.0.0
whitenoise~=6.6.0
dj-database-url~=2.1.0

# Email & Reporting
django-anymail~=11.0
weasyprint~=62.0
openpyxl~=3.1.2

# Monitoring & Metrics
prometheus-client~=0.19.0
django-prometheus~=2.3.1
sentry-sdk[django]~=1.40.0

# Performance & Load Testing
locust~=2.20.0
memory-profiler~=0.61.0

# Testing & Code Coverage
pytest~=8.3.3
pytest-django~=4.9.0
pytest-cov~=6.0.0
